.devcontainer/docker-compose.ymlにPostgreSQL追加。（要件に合わせて。MySQL等）
```yaml
version: '3.7'
services:
  nest:
    build:
      args:
        - NODE_VER=25
        ## x86_64 or aarch64
        - ARCH=aarch64
      context: .
      dockerfile: Dockerfile
    tty: true
    volumes:
      - ..:/apl
      - ../node_modules:/home/node/app/node_modules
    ports:
      - "3000:3000"

  db:
    image: postgres:17
    restart: always
    environment:
      POSTGRES_USER: devuser
      POSTGRES_PASSWORD: devpass
      POSTGRES_DB: myapp
    ports:
      - "5432:5432"
    volumes:
      - postgres-data:/var/lib/postgresql/data

volumes:
  postgres-data:
```

コンテナリビルド

PostgreSQLのドライバを追加(要件に合わせる。MySQL等)
```bash
npm install pg
```


```bash
npm install --save @nestjs/typeorm typeorm
```

```Typescript
import { Module } from '@nestjs/common';
import { TypeOrmModule } from '@nestjs/typeorm';
import { ConfigModule, ConfigService } from '@nestjs/config';
import configuration from './config/configuration';
import { validationSchema } from './config/validation';

@Module({
  imports: [
    ConfigModule.forRoot({
      isGlobal: true,
      ignoreEnvFile: true, // ← dotenv-flowで読み込むので、NestJSでは読み込まない！
      load: [configuration],
      validationSchema,
    }),
    TypeOrmModule.forRootAsync({
      imports: [ConfigModule],
      useFactory: (config: ConfigService) => ({
        type: 'postgres',
        host: config.get('database.host'),
        port: config.get('database.port'),
        username: config.get('database.user'),
        password: config.get('database.pass'),
        database: config.get('database.name'),
        entities: [__dirname + '/common/entities/*.entity{.ts,.js}'],
        synchronize: false,
      }),
      inject: [ConfigService],
    }),
  ],
})
export class AppModule {}
```

synchronize: true
アプリ起動時に自動でテーブル作成・更新される。絶対に使わないで！データ消える可能性ある！
全ての環境でマイグレーションを用いてDBにテーブル・データを流す。

```
// src/common/entities/user.entity.ts
import { Entity, PrimaryGeneratedColumn, Column } from 'typeorm';

@Entity()
export class User {
  @PrimaryGeneratedColumn()
  id: number;

  @Column()
  name: string;

  @Column({ unique: true })
  email: string;

  @Column()
  password: string;
}
```


テーブル・データを流し込む仕組みづくり


このままだと nest build しても data-source.ts はビルドされないので、
「tsconfig.build.json」に"include": ["src", "data-source.ts"]を追加
```
{
  "extends": "./tsconfig.json",
  "exclude": ["node_modules", "test", "dist", "**/*spec.ts"],
  "include": ["src", "data-source.ts"]
}
```

プロジェクト直下にdata-source.ts。
```TypeScript
// data-source.ts
import * as dotenvFlow from 'dotenv-flow';
dotenvFlow.config(); // ← NestJSと同じ読み込みルール！

import { DataSource } from 'typeorm';
import configuration from './src/config/configuration';

const db = configuration().database;

export default new DataSource({
  type: 'postgres',
  host: db.host,
  port: db.port,
  username: db.user,
  password: db.pass,
  database: db.name,
  entities: ['src/common/entities/*.entity{.ts,.js}'],
  migrations: ['src/migrations/*.ts'],
  synchronize: false,
});

```

package.jsonのscriptsにmigrationを追加。
```json
"scripts": {
    "build": "nest build",
    "format": "prettier --write \"src/**/*.ts\" \"test/**/*.ts\"",
    "start": "NODE_ENV=devcontainer nest start",
    "start:dev": "NODE_ENV=devcontainer nest start --watch",
    "start:debug": "NODE_ENV=devcontainer nest start --debug --watch",
    "start:prod": "NODE_ENV=devcontainer node dist/main",
    "lint": "eslint \"{src,apps,libs,test}/**/*.ts\" --fix",
    "test": "jest",
    "test:watch": "jest --watch",
    "test:cov": "jest --coverage",
    "test:debug": "node --inspect-brk -r tsconfig-paths/register -r ts-node/register node_modules/.bin/jest --runInBand",
    "test:e2e": "jest --config ./test/jest-e2e.json",
    "migration:generate": "NODE_ENV=devcontainer npx ts-node -r tsconfig-paths/register ./node_modules/typeorm/cli.js migration:generate src/migrations/AutoGeneratedMigration -d data-source.ts",
    "migration:run": "NODE_ENV=devcontainer npx ts-node -r tsconfig-paths/register ./node_modules/typeorm/cli.js migration:run -d data-source.ts",
    "migration:revert": "NODE_ENV=devcontainer npx ts-node -r tsconfig-paths/register ./node_modules/typeorm/cli.js migration:revert -d data-source.ts",
    "migration:run:development": "NODE_ENV=development npx ts-node -r tsconfig-paths/register ./node_modules/typeorm/cli.js migration:run -d data-source.ts",
    "migration:revert:development": "NODE_ENV=development npx ts-node -r tsconfig-paths/register ./node_modules/typeorm/cli.js migration:revert -d data-source.ts",
    "migration:run:production": "NODE_ENV=production npx ts-node -r tsconfig-paths/register ./node_modules/typeorm/cli.js migration:run -d data-source.ts",
    "migration:revert:production": "NODE_ENV=production npx ts-node -r tsconfig-paths/register ./node_modules/typeorm/cli.js migration:revert -d data-source.ts"
  },
 ```

マイグレーションファイル作成。git commit対象。
 ```
 migration:generate
 ```