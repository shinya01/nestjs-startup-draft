
```
nest new sample-project
```
npmを選択

```
shopt -s dotglob
mv sample-project/* .
shopt -u dotglob
rm -rf sample-project
```
※`./.git`が移動できなくても問題ない

```
volta pin node
volta pin npm
```

Gitレポジトリの宛先を新しいPJのレポジトリに付け替える
```
git remote rename origin old-origin
git remote rm old-origin
git remote add origin 新しいURL
git add .
git commit
git push origin master
```


