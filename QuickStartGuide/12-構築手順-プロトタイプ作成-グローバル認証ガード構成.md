# 12-構築手順-プロトタイプ作成-グローバル認証ガード構成

## 🎯 目的

NestJS アプリケーション全体に対してデフォルトで JWT 認証を適用します。個別のエンドポイントごとに `@UseGuards()` を記述する手間を省きつつ、`@Public()` デコレーターを用いることで、特定のルートのみ認証をスキップできる、セキュアかつ効率的なガード構成を実現します。

## 📂 この章で作成・修正するファイル

作業完了時には、以下のディレクトリ構成となります。

```text
src/
├── auth/
│   ├── decorators/
│   │   ├── public.decorator.ts
│   │   └── index.ts
│   ├── guards/
│   │   └── cognito-auth.guard.ts (修正)
│   └── auth.module.ts
├── user/
│   └── user.controller.ts (修正)
├── article/
│   └── article.controller.ts (修正)
└── main.ts (修正)
```

---

## 🛠️ 構築手順

### 1. `@Public()` デコレーターの作成

認証をスキップするためのメタデータを付与するカスタムデコレーターを作成します。

#### `src/auth/decorators/public.decorator.ts`

```ts
import { SetMetadata } from '@nestjs/common';

export const IS_PUBLIC_KEY = 'isPublic';
export const Public = () => SetMetadata(IS_PUBLIC_KEY, true);
```

#### `src/auth/decorators/index.ts`

```ts
export * from './public.decorator';
```

---

### 2. Guard の拡張（Reflector の導入）

`CognitoAuthGuard` を修正し、`Reflector` を使ってクラスやハンドラーに `@Public()` が設定されているかを確認するロジックを追加します。

#### `src/auth/guards/cognito-auth.guard.ts`

```ts
import {
  Injectable,
  ExecutionContext,
  UnauthorizedException,
} from '@nestjs/common';
import { AuthGuard } from '@nestjs/passport';
import { ConfigService } from '@nestjs/config';
import { Reflector } from '@nestjs/core';
import { IS_PUBLIC_KEY } from '../decorators';
import { AuthUser } from '../types';

@Injectable()
export class CognitoAuthGuard extends AuthGuard('cognito') {
  constructor(
    private configService: ConfigService,
    private reflector: Reflector,
  ) {
    super();
  }

  canActivate(context: ExecutionContext) {
    // クラス(Controller)またはメソッド(Handler)に@Public()があるかチェック
    const isPublic = this.reflector.getAllAndOverride<boolean>(IS_PUBLIC_KEY, [
      context.getHandler(),
      context.getClass(),
    ]);

    // 公開ルートの場合は認証をパス
    if (isPublic) {
      return true;
    }

    // それ以外は通常のJWT検証を実行
    return super.canActivate(context);
  }

  handleRequest<TUser = AuthUser>(err: any, user: TUser, info: any): TUser {
    const disableAuth = this.configService.get<boolean>('app.authDisable');
    if (disableAuth) {
      return { sub: 'dummy-user', iss: 'dummy' } as TUser;
    }

    if (err || !user) {
      let errorMessage = 'Authentication failed';
      if (info instanceof Error) {
        errorMessage = info.message;
      } else if (
        typeof info === 'object' &&
        info !== null &&
        'message' in info
      ) {
        errorMessage = String((info as any).message);
      } else if (typeof info === 'string') {
        errorMessage = info;
      }
      throw err instanceof Error
        ? err
        : new UnauthorizedException(errorMessage);
    }
    return user;
  }
}
```

---

### 3. グローバルガードの適用 (`main.ts`)

`main.ts` で `app.useGlobalGuards()` を使用し、アプリケーション全体にガードを適用します。

#### `src/main.ts`

```ts
// ...（前略：既存のインポート）
import { CognitoAuthGuard } from './auth/guards';

async function bootstrap() {
  // ...（中略： transactional初期化やLogger設定）

  const app = await NestFactory.create(AppModule, { bufferLogs: true });
  // ...（中略）

  const configService = app.get(ConfigService);
  const reflector = app.get(Reflector);

  // CognitoAuthGuard をグローバルガードとして設定
  app.useGlobalGuards(new CognitoAuthGuard(configService, reflector));

  const swaggerConfig = new DocumentBuilder()
    .setTitle(configService.get<string>('swagger.title') || '')
    .addBearerAuth(
      {
        type: 'http',
        scheme: 'bearer',
        bearerFormat: 'JWT',
        description: 'アクセストークンを入力してください',
      },
      'access-token',
    )
    .build();

  // ...（後略）
}
void bootstrap();
```

---

### 4. Controller の再設定

グローバルガード化したため、個別の `@UseGuards()` は不要になります。公開したいエンドポイントにのみ `@Public()` を付与します。

#### `src/user/user.controller.ts`

```ts
// ...（中略）
import { Public } from 'src/auth/decorators';

@ApiTags('Users')
@Controller('users')
@ApiErrorResponses()
@ApiBearerAuth('access-token')
@ApiAuthErrorResponses()
export class UserController {
  // ...

  @Public() // 全ユーザー取得のみ公開
  @Get()
  @ApiOperation({ summary: '全ユーザーを取得' })
  @ApiSuccessResponse({ model: UserDto, isArray: true })
  getAll() {
    return this.userService.getAll();
  }

  @Get(':id') // 認証が必要（デフォルト）
  @ApiOperation({ summary: 'IDでユーザーを取得' })
  @ApiParam({ name: 'id', description: 'ユーザーID' })
  @ApiSuccessResponse({ model: UserDto })
  getById(@Param('id') id: number) {
    return this.userService.getById(Number(id));
  }
}
```

---

## ✅ 機能一覧

| 機能 | 実装方法 |
| :--- | :--- |
| **デフォルト認証適用** | `main.ts` にて一括適用し、設定漏れを防止 |
| **認証スキップ制御** | `@Public()` デコレーターと `Reflector` による例外判定 |
| **開発用バイパス** | `.env` の `AUTH_DISABLE=true` によるスキップ対応 |

---

## 📝 参照

- [NestJS Security: Enable authentication globally](https://docs.nestjs.com/security/authentication#enable-authentication-globally)
- [NestJS Fundamentals: Execution context](https://docs.nestjs.com/fundamentals/execution-context)
