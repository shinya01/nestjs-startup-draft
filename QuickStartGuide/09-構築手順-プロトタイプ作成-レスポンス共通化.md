# 09-構築手順-プロトタイプ作成-レスポンス共通化

## 🎯 目的

NestJS の `Interceptor` を活用し、すべての成功レスポンス（200番台）を共通の JSON 形式に整形します。エラーレスポンスの共通化（第8章）と組み合わせることで、フロントエンドにとって扱いやすい一貫性のある API インターフェースを実現します。

## 📂 この章で作成・修正するファイル

作業完了時には、以下のディレクトリ構成となります。

```text
src/
├── common/
│   ├── decorators/ (既存/修正)
│   │   ├── api-paginated-response.decorator.ts
│   │   └── api-success-response.decorator.ts
│   ├── interceptors/ (新規作成)
│   │   ├── response-transform.interceptor.ts
│   │   └── index.ts
│   ├── swagger/ (既存/修正)
│   │   ├── paginated-response.dto.ts
│   │   └── success-response.dto.ts
│   └── types/ (新規作成)
│       ├── paginated-response.type.ts
│       ├── success-response.type.ts
│       └── index.ts
├── user/
│   └── user.controller.ts (修正)
├── article/
│   └── article.controller.ts (修正)
└── main.ts (修正)
```

---

## 🛠️ 構築手順

### 1. レスポンス型の定義

アプリケーション内部で扱う共通レスポンスの型を定義します。

#### `src/common/types/success-response.type.ts`

```ts
export class SuccessResponse<T> {
  success: boolean;
  statusCode: number;
  timestamp: string;
  path: string;
  data?: T;
}
```

#### `src/common/types/paginated-response.type.ts`

```ts
export interface PaginationMeta {
  total: number;
  page: number;
  limit: number;
}

export class PaginatedResponse<T> {
  success: boolean;
  statusCode: number;
  timestamp: string;
  path: string;
  data: T[];
  meta: PaginationMeta;
}
```

#### `src/common/types/index.ts`

```ts
export * from './success-response.type';
export * from './paginated-response.type';
```

---

### 2. Swagger 表示用のレスポンス DTO

APIドキュメント上で共通構造を正しく表示するための DTO を作成します。

#### `src/common/swagger/success-response.dto.ts`

```ts
import { ApiProperty } from '@nestjs/swagger';

export class SuccessResponseDto {
  @ApiProperty({ example: true })
  success: boolean;

  @ApiProperty({ example: 200 })
  statusCode: number;

  @ApiProperty({ example: '2026-01-24T12:34:56.789Z' })
  timestamp: string;

  @ApiProperty({ example: '/users' })
  path: string;

  @ApiProperty({ description: 'レスポンスデータ', type: Object, nullable: true })
  data: any;
}
```

#### `src/common/swagger/paginated-response.dto.ts`

```ts
import { ApiProperty } from '@nestjs/swagger';

class PaginationMetaDto {
  @ApiProperty({ example: 100 })
  total: number;
  @ApiProperty({ example: 1 })
  page: number;
  @ApiProperty({ example: 10 })
  limit: number;
}

export class PaginatedResponseDto {
  @ApiProperty({ example: true })
  success: boolean;

  @ApiProperty({ example: 200 })
  statusCode: number;

  @ApiProperty({ example: '2026-01-24T12:34:56.789Z' })
  timestamp: string;

  @ApiProperty({ example: '/articles' })
  path: string;

  @ApiProperty({ type: [Object] })
  data: any[];

  @ApiProperty({ type: PaginationMetaDto })
  meta: PaginationMetaDto;
}
```

#### `src/common/swagger/index.ts`

```ts
// ... 既存のエクスポート
export * from './success-response.dto';
export * from './paginated-response.dto';
```

---

### 3. Interceptor の実装

実際にレスポンスをラップするロジックを実装します。

#### `src/common/interceptors/response-transform.interceptor.ts`

```ts
import { Injectable, NestInterceptor, ExecutionContext, CallHandler } from '@nestjs/common';
import { Observable } from 'rxjs';
import { map } from 'rxjs/operators';
import { Request, Response } from 'express';
import { SuccessResponse } from '../types';

@Injectable()
export class ResponseTransformInterceptor<T = unknown> implements NestInterceptor<T, SuccessResponse<T>> {
  intercept(context: ExecutionContext, next: CallHandler<T>): Observable<SuccessResponse<T>> {
    const ctx = context.switchToHttp();
    const request: Request = ctx.getRequest();
    const response: Response = ctx.getResponse();

    return next.handle().pipe(
      map((data: T): SuccessResponse<T> => {
        const base = {
          success: true,
          statusCode: response.statusCode,
          timestamp: new Date().toISOString(),
          path: request.url,
        };
        // 204 No Content の場合は data を含めない
        return response.statusCode === 204 ? (base as SuccessResponse<T>) : { ...base, data };
      }),
    );
  }
}
```

#### `src/common/interceptors/index.ts`

```ts
export * from './response-transform.interceptor';
```

---

### 4. 共通レスポンスデコレーターの作成

Swagger の記述を簡潔にするためのカスタムデコレーターを整備します。

#### `src/common/decorators/api-success-response.decorator.ts`

```ts
import { applyDecorators, Type } from '@nestjs/common';
import {
  ApiOkResponse,
  ApiCreatedResponse,
  ApiAcceptedResponse,
  ApiNoContentResponse,
  getSchemaPath,
  ApiExtraModels,
} from '@nestjs/swagger';
import { SuccessResponseDto } from '../swagger/success-response.dto';

type StatusCode = 200 | 201 | 202 | 204;
type StatusOption = StatusCode | 'auto';

interface ApiSuccessResponseOptions<TModel extends Type<unknown>> {
  model: TModel;
  isArray?: boolean;
  description?: string;
  statusCode?: StatusOption;
}

const statusToDecorator = {
  200: ApiOkResponse,
  201: ApiCreatedResponse,
  202: ApiAcceptedResponse,
  204: ApiNoContentResponse,
} as const;

export function ApiSuccessResponse<TModel extends Type<unknown>>(
  options: ApiSuccessResponseOptions<TModel>,
) {
  const {
    model,
    isArray = false,
    description = '成功レスポンス',
    statusCode = 'auto',
  } = options;

  const resolvedStatus: StatusCode =
    statusCode === 'auto'
      ? model.name.toLowerCase().includes('create') ||
        model.name.toLowerCase().includes('post')
        ? 201
        : 200
      : statusCode;

  const responseDecorator = statusToDecorator[resolvedStatus] ?? ApiOkResponse;

  const schema =
    resolvedStatus === 204
      ? {}
      : {
          allOf: [
            { $ref: getSchemaPath(SuccessResponseDto) },
            {
              properties: {
                data: isArray
                  ? {
                      type: 'array',
                      items: { $ref: getSchemaPath(model) },
                      nullable: true,
                    }
                  : {
                      allOf: [{ $ref: getSchemaPath(model) }],
                      nullable: true,
                    },
              },
            },
          ],
        };

  return applyDecorators(
    ApiExtraModels(SuccessResponseDto, model),
    responseDecorator({
      description,
      schema,
    }),
  );
}
```

#### `src/common/decorators/index.ts`

```ts
// ... 既存のエクスポート
export * from './api-success-response.decorator';
```

---

### 5. Controller への適用

#### `src/user/user.controller.ts` / `src/article/article.controller.ts`

`@ApiResponse` を `@ApiSuccessResponse` に置き換えます。

```ts
@Get()
@ApiOperation({ summary: '全ユーザーを取得' })
@ApiSuccessResponse({ model: UserDto, isArray: true })
getAll() {
  return this.userService.getAll();
}

@Post()
@ApiOperation({ summary: 'ユーザーを作成' })
@ApiSuccessResponse({ model: UserDto, statusCode: 201 })
create(@Body() body: CreateUserDto) {
  return this.userService.create(body);
}
```

---

### 6. `main.ts` でのグローバル適用

#### `src/main.ts`

```ts
// ... imports
import { ResponseTransformInterceptor } from './common/interceptors';
import { SuccessResponseDto, ErrorResponseDto } from './common/swagger';

async function bootstrap() {
  // ...
  const app = await NestFactory.create(AppModule, { bufferLogs: true });

  // レスポンス共通化インターセプターの適用
  app.useGlobalInterceptors(new ResponseTransformInterceptor());
  app.useGlobalFilters(new HttpExceptionFilter());

  // ... Swagger Config
  const document = SwaggerModule.createDocument(app, swaggerConfig, {
    extraModels: [SuccessResponseDto, ErrorResponseDto],
  });
  // ...
}
```

---

## ✅ 完了確認

- [ ] `GET /users` を実行し、結果が `{ "success": true, "statusCode": 200, "data": [...] }` という構造で返ることを確認。
- [ ] `POST /users` を実行し、`statusCode: 201` の共通構造で返ることを確認。
- [ ] Swagger UI 上で、Response のシミュレーション値が共通構造（SuccessResponseDto）になっていることを確認。

## 📌 補足ポイント

- **Interceptor の役割**: Controller が返却した生のデータ（EntityやDTO）を、送信直前に加工します。
- **204 No Content への対応**: 削除処理などでデータがない場合、`data: null` を送らずメタ情報のみを返すよう制御しています。
- **保守性**: レスポンス形式を将来変更する場合（例：APIバージョン情報の追加など）、この Interceptor を一箇所直すだけで全エンドポイントに反映されます。

## 📝 参照

- [NestJS Interceptors](https://docs.nestjs.com/interceptors)
- [NestJS Custom Decorators](https://docs.nestjs.com/custom-decorators)
