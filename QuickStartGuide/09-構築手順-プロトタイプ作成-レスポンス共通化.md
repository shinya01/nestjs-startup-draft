# 09-構築手順 - プロトタイプ作成 - レスポンス整形（Interceptor）

## 🎯 目的

NestJS の Interceptor を使って、**すべての成功レスポンスを共通の形式に整形**。  
これにより、クライアント側でのレスポンス処理が一貫し、開発効率と保守性を向上。

---

## 🧾 共通レスポンス形式（成功時）

```json
{
  "success": true,
  "statusCode": 200,
  "timestamp": "2026-01-24T12:34:56.789Z",
  "path": "/users",
  "data": { ... }
}
```

---

## 🧾 共通レスポンス形式（ページネーション対応）

```json
{
  "success": true,
  "statusCode": 200,
  "timestamp": "2026-01-24T12:34:56.789Z",
  "path": "/articles",
  "data": [ ... ],
  "meta": {
    "total": 100,
    "page": 1,
    "limit": 10
  }
}
```

---

## 📁 ディレクトリ構成（追加）

```
src/
├── common/
│   ├── interceptors/
│   │   ├── index.ts
│   │   └── response-transform.interceptor.ts
│   ├── swagger/
│   │   ├── index.ts
│   │   ├── success-response.dto.ts
│   │   └── paginated-response.dto.ts
│   ├── types/
│   │   ├── index.ts
│   │   ├── success-response.type.ts
│   │   └── paginated-response.type.ts
│   ├── decorators/
│   │   ├── index.ts
│   │   ├── api-success-response.decorator.ts
│   │   └── api-paginated-response.decorator.ts
```

---

## 🧩 共通モジュールの index.ts 追加

各共通ディレクトリに `index.ts` を配置することで、インポートの簡略化と保守性を向上。

### `interceptors/index.ts`

```ts
export * from './response-transform.interceptor';
```

### `swagger/index.ts`

```ts
export * from './success-response.dto';
export * from './paginated-response.dto';
```

### `types/index.ts`

```ts
export * from './success-response.type';
export * from './paginated-response.type';
```

### `decorators/index.ts`

```ts
export * from './api-success-response.decorator';
export * from './api-paginated-response.decorator';
```

---

## 🧩 型安全なレスポンス型の定義

```ts
// src/common/types/success-response.type.ts
export class SuccessResponse<T> {
  success: boolean;
  statusCode: number;
  timestamp: string;
  path: string;
  data?: T;
}
```

```ts
// src/common/types/paginated-response.type.ts
export interface PaginationMeta {
  total: number;
  page: number;
  limit: number;
}

export class PaginatedResponse<T> {
  success: boolean;
  statusCode: number;
  timestamp: string;
  path: string;
  data: T[];
  meta: PaginationMeta;
}
```

---

## 🧩 Swagger 表示用のレスポンス DTO

```ts
// src/common/swagger/success-response.dto.ts
import { ApiProperty, ApiExtraModels } from '@nestjs/swagger';

@ApiExtraModels()
export class SuccessResponseDto {
  @ApiProperty({ example: true })
  success: boolean;

  @ApiProperty({ example: 200 })
  statusCode: number;

  @ApiProperty({ example: '2026-01-24T12:34:56.789Z' })
  timestamp: string;

  @ApiProperty({ example: '/users' })
  path: string;

  @ApiProperty({
    description: 'レスポンスデータ',
    type: Object,
    nullable: true,
  })
  data: any;
}
```

```ts
// src/common/swagger/paginated-response.dto.ts
import { ApiProperty } from '@nestjs/swagger';

class PaginationMetaDto {
  @ApiProperty({ example: 100 })
  total: number;

  @ApiProperty({ example: 1 })
  page: number;

  @ApiProperty({ example: 10 })
  limit: number;
}

export class PaginatedResponseDto {
  @ApiProperty({ example: true })
  success: boolean;

  @ApiProperty({ example: 200 })
  statusCode: number;

  @ApiProperty({ example: '2026-01-24T12:34:56.789Z' })
  timestamp: string;

  @ApiProperty({ example: '/articles' })
  path: string;

  @ApiProperty({ type: [Object] })
  data: any[];

  @ApiProperty({ type: PaginationMetaDto })
  meta: PaginationMetaDto;
}
```

---

## 🧩 Interceptor の実装

> 成功レスポンスを共通形式に整形し、クライアント側の処理を簡素化

```ts
// src/common/interceptors/response-transform.interceptor.ts
import {
  Injectable,
  NestInterceptor,
  ExecutionContext,
  CallHandler,
} from '@nestjs/common';
import { Observable } from 'rxjs';
import { map } from 'rxjs/operators';
import { Request, Response } from 'express';
import { SuccessResponse } from '../types';

@Injectable()
export class ResponseTransformInterceptor<
  T = unknown,
> implements NestInterceptor<T, SuccessResponse<T>> {
  intercept(
    context: ExecutionContext,
    next: CallHandler<T>,
  ): Observable<SuccessResponse<T>> {
    const ctx = context.switchToHttp();
    const request: Request = ctx.getRequest();
    const response: Response = ctx.getResponse();

    return next.handle().pipe(
      map((data: T): SuccessResponse<T> => {
        const base = {
          success: true,
          statusCode: response.statusCode,
          timestamp: new Date().toISOString(),
          path: request.url,
        };
        return response.statusCode === 204
          ? (base as SuccessResponse<T>)
          : { ...base, data };
      }),
    );
  }
}
```

---

## 🚀 `main.ts` の設定

> トランザクションコンテキスト、バリデーション、例外フィルター、Swagger ドキュメントの初期化設定

```ts
// src/main.ts
import {
  initializeTransactionalContext,
  StorageDriver,
} from 'typeorm-transactional';
import 'dotenv-flow/config';

import { NestFactory } from '@nestjs/core';
import { AppModule } from './app.module';
import { Logger } from 'nestjs-pino';
import { ConfigService } from '@nestjs/config';
import { DocumentBuilder, SwaggerModule } from '@nestjs/swagger';
import { ValidationPipe } from '@nestjs/common';
import { HttpExceptionFilter } from './common/filters';
import { ResponseTransformInterceptor } from './common/interceptors';
import { ErrorResponseDto } from './common/swagger;
import { SuccessResponseDto } from './common/swagger';

async function bootstrap() {
  // トランザクションコンテキストの初期化
  initializeTransactionalContext({ storageDriver: StorageDriver.AUTO });

  const app = await NestFactory.create(AppModule, { bufferLogs: true });
  app.useLogger(app.get(Logger));

  app.useGlobalPipes(
    new ValidationPipe({
      whitelist: true,
      transform: true,
    }),
  );

  app.useGlobalFilters(new HttpExceptionFilter());

  const configService = app.get(ConfigService);

  const swaggerConfig = new DocumentBuilder()
    .setTitle(configService.get<string>('swagger.title') || 'My API')
    .setDescription(
      configService.get<string>('swagger.description') || 'API documentation',
    )
    .setVersion(configService.get<string>('swagger.version') || '1.0')
    .build();

  const document = SwaggerModule.createDocument(app, swaggerConfig, {
    extraModels: [SuccessResponseDto, ErrorResponseDto],
  });

  SwaggerModule.setup('swagger', app, document);

  const port = configService.get<number>('app.port') || 3000;
  await app.listen(port);
}
void bootstrap();
```

---

## 🚀 `main.ts` でのグローバル登録

```ts
// src/main.ts（抜粋）
import { ResponseTransformInterceptor } from './common/interceptors';

app.useGlobalInterceptors(new ResponseTransformInterceptor());
```

---

## 🧩 共通レスポンスデコレーターの作成

### ページネーション対応レスポンス用

```ts
// src/common/decorators/api-paginated-response.decorator.ts
import { applyDecorators, Type } from '@nestjs/common';
import { ApiOkResponse, getSchemaPath, ApiExtraModels } from '@nestjs/swagger';
import { PaginatedResponseDto } from '../swagger';

export function ApiPaginatedResponse<TModel extends Type<unknown>>(
  model: TModel,
  description = 'ページネーション付きレスポンス',
) {
  return applyDecorators(
    ApiExtraModels(PaginatedResponseDto, model),
    ApiOkResponse({
      description,
      schema: {
        allOf: [
          { $ref: getSchemaPath(PaginatedResponseDto) },
          {
            properties: {
              data: {
                type: 'array',
                items: { $ref: getSchemaPath(model) },
              },
            },
          },
        ],
      },
    }),
  );
}
```

---

## 🧪 Controller 側の使用例（ページネーション）

```ts
// src/article/article.controller.ts
@Get()
@ApiOperation({ summary: '記事一覧（ページネーション）' })
@ApiPaginatedResponse(ArticleDto)
getPaginated(@Query() query: GetArticlesQueryDto) {
  return this.articleService.getPaginated(query);
}
```

---

## ✅ 補足ポイント

- `ResponseTransformInterceptor` によって、**成功レスポンスを共通構造に整形**
- `SuccessResponse` / `PaginatedResponse` によって、**型安全なレスポンスを実現**
- `@ApiPaginatedResponse()` によって、**Swagger UI にも正確な構造を表示**
- `Interceptor` は `data` + `meta` をそのまま包むだけで実装可能
- `meta` の構造は必要に応じて拡張可能（例：`hasNext`, `totalPages`, `sort` など）
- 各共通ディレクトリに `index.ts` を作成することで、**インポートの簡略化と保守性を向上**

---
