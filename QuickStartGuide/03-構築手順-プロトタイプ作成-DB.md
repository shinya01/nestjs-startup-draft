# 03-構築手順 - プロトタイプ作成 - DB

## 🐘 PostgreSQL の導入（Docker）

`.devcontainer/docker-compose.yml` に PostgreSQL サービスの追加：

```yaml
version: '3.7'
services:
  nest:
    build:
      context: .
      dockerfile: Dockerfile
      args:
        - NODE_VER=25
        - ARCH=aarch64 # x86_64 または aarch64 を指定
    tty: true
    volumes:
      - ..:/apl
      - ../node_modules:/home/node/app/node_modules
    ports:
      - "3000:3000"

  db:
    image: postgres:17
    restart: always
    environment:
      POSTGRES_USER: devuser
      POSTGRES_PASSWORD: devpass
      POSTGRES_DB: myapp
    ports:
      - "5432:5432"
    volumes:
      - postgres-data:/var/lib/postgresql/data

volumes:
  postgres-data:
```

> 💡 PostgreSQL のバージョンは安定版（例：17）の指定がおすすめ。

---

## 🔄 コンテナのリビルド

設定反映のための DevContainer の再ビルド。

---

## 📦 TypeORM と PostgreSQL ドライバのインストール

```bash
npm install --save pg
npm install --save @nestjs/typeorm typeorm
```

---

## ⚙️ TypeORM の設定（AppModule）

```ts
// src/app.module.ts
import { Module } from '@nestjs/common';
import { TypeOrmModule } from '@nestjs/typeorm';
import { ConfigModule, ConfigService } from '@nestjs/config';
import configuration from './config/configuration';
import { validationSchema } from './config/validation';

@Module({
  imports: [
    ConfigModule.forRoot({
      isGlobal: true,
      ignoreEnvFile: true,
      load: [configuration],
      validationSchema,
    }),
    TypeOrmModule.forRootAsync({
      imports: [ConfigModule],
      useFactory: (config: ConfigService) => ({
        type: 'postgres',
        host: config.get('database.host'),
        port: config.get('database.port'),
        username: config.get('database.user'),
        password: config.get('database.pass'),
        database: config.get('database.name'),
        entities: [__dirname + '/common/entities/*.entity{.ts,.js}'],
        synchronize: false, // 必ず false を指定
      }),
      inject: [ConfigService],
    }),
  ],
})
export class AppModule {}
```

> ⚠️ `synchronize: true` の使用は非推奨。アプリ起動時にテーブルが自動作成・更新されるが、本番環境では意図しないデータ削除のリスクがあるため、すべての環境でマイグレーションによる管理を推奨。

---

## 🧱 Entity の作成（User, UserInfo, Article）

### `user.entity.ts`

```ts
// src/common/entities/user.entity.ts
import { Entity, PrimaryGeneratedColumn, Column, OneToMany, OneToOne, JoinColumn } from 'typeorm';
import { Article } from './article.entity';
import { UserInfo } from './user-info.entity';

@Entity()
export class User {
  @PrimaryGeneratedColumn()
  id: number;

  @Column({ unique: true })
  email: string;

  @Column({ unique: true, nullable: true })
  externalId: string;

  @OneToMany(() => Article, (article) => article.author)
  articles: Article[];

  @OneToOne(() => UserInfo, (info) => info.user, {
    cascade: true,
    nullable: true,
    eager: true,
  })
  @JoinColumn()
  info?: UserInfo;
}
```

---

### `user-info.entity.ts`

```ts
// src/common/entities/user-info.entity.ts
import { Entity, PrimaryGeneratedColumn, Column, OneToOne } from 'typeorm';
import { User } from './user.entity';

@Entity()
export class UserInfo {
  @PrimaryGeneratedColumn()
  id: number;

  @Column()
  name: string;

  @OneToOne(() => User, (user) => user.info)
  user: User;
}
```

---

### `article.entity.ts`

```ts
// src/common/entities/article.entity.ts
import { Entity, PrimaryGeneratedColumn, Column, ManyToOne } from 'typeorm';
import { User } from './user.entity';

@Entity()
export class Article {
  @PrimaryGeneratedColumn()
  id: number;

  @Column()
  title: string;

  @Column()
  content: string;

  @ManyToOne(() => User, (user) => user.articles, { nullable: false })
  author: User;
}
```

---

### `index.ts`

```ts
// src/common/entities/index.ts
import { User } from './user.entity';
import { Article } from './article.entity';
import { UserInfo } from './user-info.entity';

export const ENTITIES = [User, Article, UserInfo];
export { User, Article, UserInfo };
```

---

## 🛠️ マイグレーションの準備

### `tsconfig.build.json` に `data-source.ts` を追加

```json
{
  "extends": "./tsconfig.json",
  "exclude": ["node_modules", "test", "dist", "**/*spec.ts"],
  "include": ["src", "data-source.ts"]
}
```

---

### プロジェクトルートに `data-source.ts` を作成

```ts
import * as dotenvFlow from 'dotenv-flow';
dotenvFlow.config();

import { DataSource } from 'typeorm';
import configuration from './src/config/configuration';

const db = configuration().database;

export default new DataSource({
  type: 'postgres',
  host: db.host,
  port: db.port,
  username: db.user,
  password: db.pass,
  database: db.name,
  entities: [__dirname + '/src/common/entities/*.entity{.ts,.js}'],
  migrations: [__dirname + '/src/migrations/*.ts'],
  synchronize: false,
});
```

---

## 🧩 `package.json` にマイグレーション用スクリプトを追加

```json
"scripts": {
  "migration:generate": "NODE_ENV=devcontainer npx ts-node -r tsconfig-paths/register ./node_modules/typeorm/cli.js migration:generate src/migrations/AutoGeneratedMigration -d data-source.ts",
  "migration:create": "NODE_ENV=devcontainer ts-node -r tsconfig-paths/register ./node_modules/typeorm/cli.js migration:create src/migrations/SeedData",
  "migration:run": "NODE_ENV=devcontainer npx ts-node -r tsconfig-paths/register ./node_modules/typeorm/cli.js migration:run -d data-source.ts",
  "migration:revert": "NODE_ENV=devcontainer npx ts-node -r tsconfig-paths/register ./node_modules/typeorm/cli.js migration:revert -d data-source.ts"
}
```

---

## 🌱 Seed データの投入（マイグレーション方式）

### `src/migrations/xxxxxx-SeedData.ts` の作成

```ts
import { MigrationInterface, QueryRunner } from 'typeorm';

export class SeedData1700000000000 implements MigrationInterface {
  public async up(queryRunner: QueryRunner): Promise<void> {
    const micaUser = await queryRunner.query(`
      INSERT INTO "user" (email, "externalId")
      VALUES ('mica@fox.jp', NULL)
      RETURNING id
    `);
    const erinUser = await queryRunner.query(`
      INSERT INTO "user" (email, "externalId")
      VALUES ('erin@forest.jp', NULL)
      RETURNING id
    `);
    const aquaUser = await queryRunner.query(`
      INSERT INTO "user" (email, "externalId")
      VALUES ('aqua@river.jp', NULL)
      RETURNING id
    `);

    const micaId = micaUser[0].id;
    const erinId = erinUser[0].id;
    const aquaId = aquaUser[0].id;

    await queryRunner.query(`
      INSERT INTO "user_info" (name, "userId")
      VALUES
        ('ミカ', ${micaId}),
        ('エリン', ${erinId}),
        ('アクア', ${aquaId})
    `);

    await queryRunner.query(`
      INSERT INTO "article" (title, content, "authorId")
      VALUES
        ('はじめてのNestJS', 'NestJSはとっても楽しい！', ${micaId}),
        ('PostgreSQL入門', 'データベースの基本を学ぼう！', ${erinId})
    `);
  }

  public async down(queryRunner: QueryRunner): Promise<void> {
    await queryRunner.query(`DELETE FROM "article"`);
    await queryRunner.query(`DELETE FROM "user_info"`);
    await queryRunner.query(`DELETE FROM "user"`);
  }
}
```

---

### 実行方法

```bash
npm run migration:run
```

> DB にテーブルを作成し、Seed データを投入。

---

### 取り消し方法（必要に応じて）

```bash
npm run migration:revert
```

> 最後に実行したマイグレーションを取り消し。

---

## ✅ 補足ポイント（マイグレーションSeed）

- `RETURNING id` を使うことで、自動採番された ID を即時取得可能  
- 取得した ID を使って、UserInfo や Article に正確なリレーションを構築  
- `down()` では依存関係の逆順で削除し、整合性を保つ  
- エンティティは使わず、SQL ベースで記述するのがマイグレーションの基本  
- 本番環境での初期データ投入に適している構成  

---
